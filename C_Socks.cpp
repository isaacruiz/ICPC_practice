#include<iostream>
using namespace std;

int main()
{
	const int SIZE = 200000;
	int n; //number of socks
	int m; //number of days
	int k; //number of available colors
	int c [SIZE]; //color of sock at index
	int pairs [2][SIZE];

/* 	cin >> n >> m >> k;

	for (int i = 0; i < n; i++)
		cin >> c[i];

	for (int i = 0; i < m; i++)
		cin >> pairs[0][i] >> pairs[1][i]; */

	return 0;
}